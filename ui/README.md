# Chromazen controls

This directory contains the web-based control surface. The native Rust runtime remains responsible
for pointer input, pressure, stroke smoothing, stamp generation, wgpu rendering, and presentation.
