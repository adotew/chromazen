[package]
name = "chromazen-canvas"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bytemuck = { version = "1.16", features = ["derive"] }
image = { version = "0.25", default-features = false }
log = "0.4"
wgpu = "29.0.3"

[dev-dependencies]
pollster = "0.4"
